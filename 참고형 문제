퀴즈1
1. Number("17" + "33")
자료형: ? 
값: ? 

2. "안녕" + "+하세요+" + String(17 + 3)
자료형 : ?
값 : ?

3. typeof 33 + "30"
자료형 : ?
값 : ?


퀴즈2
함수 abc_string을 만든다.
1. 매개 변수 a,b,c를 받는다.
(a, b, c에는 모두 숫자만 있는 것으로 가정한다.)
2. 함수 안에 변수 ab_string을 만든다.
3. 변수 ab_string에 매개변수 a, b를 더하고 String으로 바꾸어 할당한다.
4. 함수 안에 변수 c_string을 만든다.
5. 변수 c_string에 매개변수 c를 String으로 바꾸어 할당한다.
6. 함수의 결과값으로 ab_string + c_string 반환한다.
다음을 완성하는 함수를 만들어 주세요.



퀴즈2
함수 abc_string을 만든다.
1. 매개 변수 a,b,c를 받는다.
(a, b, c에는 모두 숫자만 있는 것으로 가정한다.)
2. 함수 안에 변수 ab_string을 만든다.
3. 변수 ab_string에 매개변수 a, b를 더하고 반환한다.
4. 함수 안에 변수 c_string을 만든다.
5. 변수 c_string에 매개변수 c를 String으로 바꾸어 할당한다.
6. 함수의 결과값으로 ab_string + c_string 반환한다.
다음을 완성하는 함수를 만들어 주세요.



for문을 만들어야한다.
i는 0부터 시작해서, 11보다 작을 때 까지 반복한다.
i가 2일 때는 continue로 넘긴다.
i가 3,7일떄는 console.log('3과 7입니다')
i가 9일 때 break 한다.


함수 이름 type_check를 만든다.

매개변수로 a를 받아서,
a가 자료형이 숫자라면
for문을 만들어서 a만큼 반복문을 돌려야한다.
반복문의 코드 블럭은 console.log('반복중: '+ i) 이다.
모든 반복이 끝나면 "숫자 반복문 끝"을 return 한다.

a가 자료형이 문자열이라면
객체를 만들고,
그 객체에 text라는 key에 a를 value로 담아서, return한다.

/*
함수 print_numbers를 만듭니다.
숫자 n을 매개변수로 받습니다.
for 문을 사용하여 1부터 n까지의 숫자를 출력 합니다.
*/

/*
함수 add_two_numbers를 만듭니다.
두 개의 숫자 a와 b를 매개변수로 받습니다.
함수에서 a와 b를 더한 값을 반환합니다.
*/

함수 sum_array를 만듭니다.
숫자로 이루어진 배열 arr을 입력받습니다.
for 문을 사용하여 배열 안의 모든 숫자를 더하고, 그 결과를 반환합니다.


함수 are_numbers_equal을 만듭니다.
두 개의 숫자 a와 b를 파라미터로 받습니다.
if 문을 사용하여:
만약 두 숫자가 같으면 "They are equal"을 반환합니다.
그렇지 않으면 "They are not equal"을 반환합니다.
